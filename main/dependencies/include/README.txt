Copy the following files into this folder:

For c-ares (1.7.0 or newer):
  ares.h
  ares_build.h
  ares_rules.h
  ares_version.h

For OpenSSL:
	The "openssl" directory from OpenSSL's include dir.

Resulting directory structure:
	includes\ares.h
	includes\ares...
	includes\openssl\crypto.h
	includes\openssl\...